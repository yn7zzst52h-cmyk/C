#include <stdio.h>
#include <math.h> // serve para baixar a biblioteca de matematica com funcoes que ajudam em calculos

int main() {
    printf("%f\n", pow(2, 3)); //vai fazer 2 elevado a 3
    printf("%f\n", sqrt(36)); // raiz de 36
    printf("%f\n", ceil(36.4)); // arredonda para cima
    printf("%f\n", floor(36.7)); // arredonda para baixo
    // existem muitas mais, pesquisar!!!!!!
    getchar();
    return 0;
}